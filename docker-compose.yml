version: "2"
services:
  app:
    build:
      context: .
    command: "bash -c 'bundle && bundle exec ruby generate.rb > emoji.fish && chmod 755 emoji.fish'"
    volumes:
      - .:/emoji.fish
      - bundle-data:/usr/local/bundle/
volumes:
  bundle-data:
    driver: local
